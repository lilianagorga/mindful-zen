<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Users</title>
</head>
<body>
  <%- include('partials/header') %>
  <h1>Users List</h1>
  <ul>
    <% users.forEach(user => { %>
      <li><%= user.email %> - <%= user.firstName %> <%= user.lastName %></li>
    <% }) %>
  </ul>
  <%- include('partials/footer') %>
</body>
</html>