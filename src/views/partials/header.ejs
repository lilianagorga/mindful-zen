<header>
  <nav>
    <p>Welcome to Mindful Zen!</p>
    <% if (currentUser) { %>
      <p>Welcome, <%= currentUser.name ? currentUser.name : 'User' %>!</p>
      <a href="/logout">Logout</a>
    <% } else { %>
      <a href="/login">Login</a>
      <a href="/register">Register</a>
    <% } %>
  </nav>
</header>